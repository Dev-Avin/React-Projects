// Database.js
import React from 'react';

const Database = () => {
  const styles = {
    container: {
      padding: '20px',
      backgroundColor: '#fcf0c7',
    },
    heading: {
      color: 'orange',
    },
  };

  return (
    <div style={styles.container}>
      <h2 style={styles.heading}>Database Page</h2>
      <p>This is the Database page content.</p>
    </div>
  );
};

export default Database;
